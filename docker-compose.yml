version: "3.9"

services:
  web:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - redis
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Redis__ConnectionString=redis:6379

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
